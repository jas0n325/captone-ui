import { IPinRules } from "@aptos-scp/scp-component-store-selling-features";

export interface TenderExchangeScreenProps {
  accountNumber: string;
  uiInputSource: string;
  pinRules: IPinRules;
  accountNumberFieldData: string;
}
