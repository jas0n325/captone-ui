export enum ReportType {
  PrintZReport = "printZReport",
  PrintXReport = "printXReport",
  Report = "Report"
}

export enum HeaderSyncType {
  ZReport = "Z Report",
  XReport = "X Report",
  SyncData = "Sync data"
}
export enum FiscalReportToast {
  XReport  = "X-Report",
  ZReport = "Z-Report"
}

export enum FiscalReportStatus {
  Success = "Success",
  Error = "Error"
}
export enum HeaderReportType {
  ZReport = "Z Report",
  XReport = "X Report"
}

export enum FiscalDeviceWorkingStatus {
  No = "0",
  Yes = "1"
}

export enum FiscalDailyStatus {
  Closed = "0",
  Open = "1"
}
export const FiscalPrinterSpinnerTimeout = 250;
